/*const discord = require('discord.js');
const userdata = require('../userdata.js');
const fs = require("fs")
module.exports.run=(msg,bot,args)=>{
    let money = userdata.getUserData(msg.author.id).money
    
    collector.on('collect', m => {
        if(m.content<money&&m.content){}
    }
}
    



*/
module.exports.help = {
    'name': 'Trade',
    'description': 'Wymieniaj się ze znajomymi.'
};

